levels 파일에 저장할 것들
1.레벨을 표시할 순서와 점수
2.열리지 않은 레벨은 표시하지 않는다(이름을 빼버리는 방식으로)

MAP 파일에 저장할 것들

1. 지도 생김새
2. 적군시작 위치
3. 웨이브 마다 나오는 적군 배열(최대 5개)
4. 주어진 돈
5. 초기 성의 체력
6. 게임 이벤트;"rscfOLx

몹이 나오는 시간 간격(ms)
	500 250 125
	속1 속2 속3  

  이벤트를 정렬해서 파일에 저장하고
이를 한줄씩 읽어가면서 게임을 진행한다. 예를 들면 이런것.

; ;을 쓰면 이 뒤는 읽지 않는다.
; "을 쓴 뒤에는 비서가 전하는 말
; r 길파기 액션
; s 게임 시작
; c 클리어하면 아래를 읽는다. 아니면 l(else)가 나올 때까지 읽어내려간다.
; f 숫자 별을 숫자개 이상 먹었을 때 이벤트. c 아니면 f중 하나만 쓸것.
; O 병사 종류를 연다
; L 레벨을 연다.
; x를 하면 더이상 읽지 않고 패스.


2016-12-04 길파기 알고리즘 제작중

2016-12-04 전체적으로 map 저장 방식에 있던 오류를 다듬었음.
이제 상하좌우 헷갈릴 필요 없이 쓰면 된다.
>>map[a][b]에서 a가 위아래를, b가 왼쪽 오른쪽을 맡는게 무조건적으로 깔끔하다.

2016-12-05 길파기 알고리즘 완성 및 길을 만들지 못하던 등의 버그를 고침.
이제 본게임이랑 순서도 파일 읽는것만 남았으니
먼저 본게임을 손보고 순서도 파일 읽는걸 건들 예정.